(function () {
    'use strict';

    // モジュール定義
    ons.bootstrap('app', ['onsen']);

    // 実行時のアプリ初期化処理
    ons.ready(function () {
        console.log("Onsen UI is ready!");
    });

})();